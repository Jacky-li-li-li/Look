// ============================================================
// Electron Builder config
// ============================================================

module.exports = {
  appId: "com.agent-harness.app",
  publish: {
    provider: "github",
    owner: "Jacky-li-li-li",
    repo: "Look",
  },
  productName: "Look",
  directories: {
    output: "release",
    buildResources: "build",
  },
  files: [
    "dist/**/*",
    "node_modules/**/*",
    "package.json",
  ],
  // Ship icons alongside the app for each platform.
  // build/ is the default buildResources dir, so paths are relative to it.
  mac: {
    icon: "icon.icns", // multi-resolution Apple icon (16/32/64/128/256/512/1024)
    category: "public.app-category.developer-tools",
    target: ["dmg", "zip"],
  },
  win: {
    icon: "icon.ico",
    target: ["nsis"],
  },
  linux: {
    icon: "icon.png",
    target: ["AppImage", "deb"],
  },
};
