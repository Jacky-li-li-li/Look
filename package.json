{
	"name": "look",
	"version": "1.2.4",
	"description": "Electron desktop agent app — Look",
	"private": true,
	"type": "module",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"engines": {
		"node": ">=20.19.0"
	},
	"scripts": {
		"postinstall": "npm run copy-file-icons --workspace=@look/electron && patch-package",
		"dev": "npm run dev --workspace=@look/electron",
		"copy-file-icons": "npm run copy-file-icons --workspace=@look/electron",
		"dev:renderer": "npm run dev:renderer --workspace=@look/electron",
		"dev:main": "npm run dev:main --workspace=@look/electron",
		"build": "npm run build --workspace=@look/shared && npm run build --workspace=@look/electron",
		"build:shared": "npm run build --workspace=@look/shared",
		"build:main": "npm run build:main --workspace=@look/electron",
		"build:renderer": "npm run build:renderer --workspace=@look/electron",
		"stage:production": "npm run stage:production --workspace=@look/electron",
		"package:prepare": "npm run build && npm run stage:production --workspace=@look/electron",
		"start": "npm run start --workspace=@look/electron",
		"package": "npm run build && npm run package --workspace=@look/electron",
		"package:mac:notarized": "npm run build && npm run package:mac:notarized --workspace=@look/electron",
		"test": "npm run test --workspace=@look/electron",
		"test:watch": "npm run test:watch --workspace=@look/electron",
		"lint": "biome check apps/electron/src packages",
		"lint:fix": "biome check --write --unsafe apps/electron/src packages",
		"format": "biome format --write apps/electron/src apps/electron/test packages",
		"check": "npm run lint && npm run typecheck --workspace=@look/shared && npm run typecheck --workspace=@look/electron && npm test"
	}
}
