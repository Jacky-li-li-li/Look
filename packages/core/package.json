{
  "name": "@look/core",
  "version": "0.0.1",
  "private": true,
  "description": "Core domain logic and contracts for Look (session runtime, extensions, permissions)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@look/shared": "*",
    "@earendil-works/pi-agent-core": "^0.80.3",
    "@earendil-works/pi-ai": "^0.80.3",
    "@earendil-works/pi-coding-agent": "^0.80.3",
    "typescript": "^5.6.0"
  }
}
