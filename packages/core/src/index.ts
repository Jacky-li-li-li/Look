// Core domain package — placeholder entry point.
//
// Long-term: session runtime, extensions, permissions, and other main-process
// domain logic that does not depend on Electron APIs should migrate here from
// src/main so they can be tested and reused outside of the Electron app.
//
// For now, the application still imports these modules directly from
// src/main; this package exists to establish the monorepo boundary.
export {};
