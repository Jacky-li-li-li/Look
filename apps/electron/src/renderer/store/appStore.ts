// ============================================================
// Global Jotai store shared by IPC handler and React Provider.
// ============================================================

import { createStore } from "jotai";

export const appStore = createStore();
