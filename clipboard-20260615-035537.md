# Look 项目全面技术审查报告

> 审查日期：2026-06-15 审查范围：Look v1.0.0（Electron + React + Vite + pi SDK 0.79.1，14883 行 TS/TSX） 审查维度：架构合规性 + 模块质量 + Bug 排查 + SDK 集成兼容性 审查方法：3 个 SubAgent 并行深度扫描 + 主上下文整合 报告文件：`workspace-files/.context/2026-06-15-comprehensive-tech-review.md`

---

## 执行摘要

Look 整体上已经是一个**结构清晰、对 pi SDK 高度遵循**的 Electron 桌面应用，"SDK first" 原则在 LLM 调用、模型注册、认证、Session 持久化等核心链路上贯彻到位。但在以下几个方面存在**真实风险**，按严重度递减：

**Top 5 整体风险**

| 排名 | 风险 | 维度 | 严重度 |
| --- | --- | --- | --- |
| 1 | **测试覆盖率近乎为零**（项目仅 3 个测试文件，14 个测试用例） | 架构/质量保障 | Blocker |
| 2 | `agents.json`**/**`projects.json` **非原子写入**，主进程崩溃可能导致整个 sidebar 空白 | 主进程数据安全 | Blocker |
| 3 | **macOS 关窗后重开，新窗口收不到任何 agent 事件流**（`registerIpcHandlers` 不重新注册） | IPC/平台兼容 | Blocker |
| 4 | `permission-gate.ts` **的全局 deny 规则可被简单字符串变体绕过**（`rm -rf /` 双空格、`(rm -rf /)`、bash 重定向写 `.env` 等） | 安全/权限 | Blocker |
| 5 | `no-sandbox` **在生产用户机器上默认启用**，等于禁用了 Electron 渲染进程沙箱 | 安全 | Critical |

**关键正面发现**

- LLM 调用 100% 走 pi SDK，无任何绕过 SDK 直接调用 provider HTTP API 的代码
- Provider 列表、API Key 存储、OAuth 自动刷新均使用 SDK 原生能力
- Jotai 状态层迁移完成，渲染层 IPC 已脱离 React 生命周期（`store/ipcHandler.ts`），原 760ms 卡顿的"状态单体"问题已修
- 三层权限门设计思路正确（global deny → role rules → protected paths）
- 双 tsconfig（main/renderer）分层合理，Vite + Electron 开发流通畅
- preload contextIsolation 正确配置

---

## 一、架构层面审查

### 1.1 进程分层（✅ 合规，2 处隐患）

三进程职责清晰：main 持有 AgentManager 单例 + 全部 SDK 调用 + 文件 IO + 权限决策；preload contextBridge 暴露 `window.look`；renderer 纯 React 视图层。`contextIsolation: true`、`nodeIntegration: false` 均正确（`src/main/index.ts:158-162`）。

**隐患 A：**`no-sandbox` **默认启用**（`src/main/index.ts:28-32`）

```ts
if (process.env.SANDBOX_GPU_WORKAROUND !== "0") {
    app.commandLine.appendSwitch("no-sandbox");
    app.commandLine.appendSwitch("disable-gpu-sandbox");
}
```

默认情况下（`SANDBOX_GPU_WORKAROUND` 未设置）始终激活，等于禁用 Chromium 进程沙箱。本意是解决 CI/Docker 无 GPU 问题，但生产用户也会触发。**整改：反转默认逻辑——仅当** `SANDBOX_GPU_WORKAROUND === "1"` **才启用。**

**隐患 B：**`window.harness` **别名遗留**（`src/main/preload.js:197-198`） 完整 IPC 表面被 `window.look` 和 `window.harness` 双暴露，注释写"将来删除"但无时间表。**整改：grep 全部** `window.harness` **调用并迁移，删除别名。**

### 1.2 pi SDK 集成模式（⚠️ 高度遵循 + 局部偏差）

**遵循部分**

- `createAgentSession()` 是 agent 创建的唯一入口（`agent-manager.ts:1196`、`createForkedSession`、`loadPersistedAgents`）
- `AuthStorage.create()` + `ModelRegistry.create()` 标准实例化（`agent-manager.ts:212-213`）
- Compaction 通过 `m.session.compact()` 委托 SDK（`agent-manager.ts:1136-1143`）
- 标题生成、provider self-test 使用 `completeSimple` 直连 pi-ai 但**保留在主进程**

**偏差 1（Major）：流式消息状态在主进程和渲染层双重维护**

- `agent-manager.ts:1685-1726` 的 `message_update` 分支中，本地维护 streaming placeholder 拼接 delta
- `renderer/store/ipcHandler.ts:299-396` 同时维护一套独立的 `message_start/update/end` 增量合并
- 经审查：主进程的本地镜像逻辑因 `message_start` 分支未把 streaming placeholder 推入 `m.messages`（仅 `message_end` 才写），导致 `[...m.messages].reverse().find(x => x.isStreaming)` 永远找不到目标 → 整段 \~40 行代码实际上是死代码
- **整改：删除主进程** `message_update` **的本地镜像逻辑，渲染层是唯一的流式状态来源**

**偏差 2（Major）：消息字段映射重复 3 次**

- `message-convert.ts:99-113` 的 `usageFromPi`：`u.input → inputTokens`
- `ipcHandler.ts:371-385`：再做一次相同映射
- `agent-manager.ts:2280-2294` 的 `trackUsage`：第三遍
- **整改：收敛到** `usageFromPi`**，其他两处复用**

**偏差 3（Minor）：自建标题生成 LLM 调用**

- `agent-manager.ts:1341-1378` 用 `completeSimple` 自建标题生成，配 `maxTokens:64, timeoutMs:15s, maxRetries:0`
- 完全可以通过 `SessionManager.inMemory()` + AgentSession 跑临时会话，享受 SDK 重试和 auth
- **整改：长期优化项，短期可保留**

### 1.3 模块间耦合度（⚠️ God Object 风险）

`agent-manager.ts` 单文件 **2462 行**，承担 11 个职责域：项目 CRUD、agent CRUD、模型解析、会话恢复、持久化、技能调用、权限模式、Usage 追踪、标题生成、分支/tree 管理、事件路由。私有状态包括 8 个 Map 实例 + 3 个标量。

**循环依赖**：未发现。AgentManager → PermissionAskService 通过回调解耦。

`shared/types.ts` **562 行**，混合领域模型（`UserProfile`、`ProjectInfo`）和 IPC 协议（`MainToRendererEvent` 400+ 行 union）。

**整改路线**

- 短期：提取 `TitleGeneratorService`、`ProjectStore`
- 中期：提取 `SessionTreeService`（消除 `extractMessagesFromSessionManager` 的 `sm: any`）
- 中期：拆分 `shared/types.ts` 为 `agent.ts`/`ipc.ts`/`project.ts`

### 1.4 数据流（⚠️ 状态发散+一致性风险）

**状态真相源在主进程**（设计正确），渲染层用 Jotai atoms 做视图层缓存，IPC 单向推送。

**风险 A：双重状态来源**（同 1.2 偏差 1）

**风险 B：乐观更新可能被 stale 快照覆盖**（`App.tsx:179-183`） `agentThinkingLevel` 乐观更新后等 `agent:updated` 事件覆盖，IPC 延迟下乐观更新可能被旧快照吞没。

**风险 C：多文件无事务写入（Blocker）**`agents.json` 和 `projects.json` 通过 `fs.writeFileSync` 分别写（`saveIndex` L516-540、`saveProjects` L249-263）。Electron 主进程被 SIGKILL/OOM 杀掉时，正在写的 JSON 可能截断 → 下次启动 `JSON.parse` 抛错 → 整个 sidebar 空白。 **整改（强烈建议）：**

```ts
const tmp = path + ".tmp";
fs.writeFileSync(tmp, JSON.stringify(data, null, 2));
fs.renameSync(tmp, path);  // 原子替换
```

### 1.5 权限流转（⚠️ 双轨道+UX 摩擦）

**风险 A：双重检查**（`agent-manager.ts:1821-1843`） `handleLookSideEffect` 在 `tool_execution_start` 中又调一次 `checkPermission`，但只处理 `deny` 不处理 `ask`（注释写"ask 由 extensionFactory 处理"）。如果 extensionFactory 因任何原因未注册成功，ask 路径会**静默失效**。 **整改：删除** `handleLookSideEffect` **的冗余 deny 检查，权限完全委托 extensionFactory。**

**风险 B：PROTECTED_PATHS 范围过宽**（`permission-gate.ts:103-110`） 任何 `.ts/.tsx/.js` 编辑都触发 ask。Look 项目本身就是 TS，coding agent 工作流被频繁打断。**整改：支持项目级** `.look/permissions.json` **豁免。**

**风险 C：并发 ask 队列（Major）**`pendingAsksAtom` 是简单数组，`drainAsk` 每次只处理第一个。多 agent 并发时 UI 没标明"当前请求属于哪个 agent"，用户在批量 allow 时可能误操作另一个 agent 的权限。

### 1.6 IPC 设计（⚠️ 流式背压+路由膨胀）

**通道设计**：`look:invoke`（请求-响应）+ `look:event`（双向 fire-and-forget），合理。

**风险 A：高频流式事件无背压**`agent:message_update` 每个 token 一次 IPC，xhigh thinking 一个长回复可能产数千事件。渲染层 `useThrottle` 减少重渲染，但 IPC 本身无节流。 **整改：主进程** `handleSessionEvent` **中对** `message_update` **做 16ms 批次合并 flush。**

**风险 B：单 mega router**`ipc-handlers.ts` 50+ case 全在一个 switch。短期可接受，中期建议按域拆分。

**风险 C：**`agent:history` **payload 大**长会话首次推送整个历史。**长期：分页推送。**

### 1.7 测试覆盖（❌ Blocker 级问题）

**当前状态**：3 个测试文件，14 个用例

- `test/permission-ask.test.ts`（2 tests）
- `test/provider-sdk-alignment.test.ts`（7 tests）
- `test/chatpanel-scroll.test.tsx`（5 tests）

**未覆盖的高风险路径**：

- `permission-gate.ts` 的 GLOBAL_DENY 规则（仅有最基础的 ask 测试）
- `message-convert.ts` 的字段映射
- IPC 路由（50+ case 全无测试）
- 重启恢复 `loadPersistedAgents`（leafId/branchSummary/fallback 都无测试）
- 无任何 E2E（Playwright/Spectron 都没有）
- 无 CI 配置（无 `.github/workflows/`）

### 1.8 SDK 升级兼容性（⚠️ 高耦合接触面多）

**升级风险高的接触面（直接消费 SDK 内部细节）**：

1. `handleSessionEvent` 的 250 行 switch-case 用 `event: any` 匹配字符串字面量（`agent-manager.ts:1660-1909`）
2. `SessionManager` 内部方法直接调用：`getBranch/getLeafId/getTree/resetLeaf/branch/appendLabelChange`
3. `extractMessagesFromSessionManager` 的 `sm: any` 全无类型保护（`agent-manager.ts:2215`）
4. `pi-ai` 底层 `completeSimple` 直接使用（标题生成、provider self-test）
5. `extensionFactories` 的 `tool_call` hook 字段（`toolCallId/toolName/input`）

**升级风险低的接触面（有抽象层）**：

- `createAgentSession` 入参（高级 API）
- `AuthStorage`/`ModelRegistry`（通过 `UserSettingsStore` 隔离）
- Skills 通过 `skill-loader.ts` 封装

**结论**：在零测试覆盖的情况下，0.80.x 升级需要人工验证至少 5 个高风险接触点。**升级前先做基础测试补全。**

---

## 二、主进程模块质量审查

### 2.1 评分总表

| 模块 | 行数 | 评分 | 主要问题 |
| --- | --- | --- | --- |
| `agent-manager.ts` | 2462 | C | God Object、内存泄漏、非原子写、`message_update` 死代码 |
| `ipc-handlers.ts` | 367 | B | 入参无运行时校验、macOS 重建窗口失效 |
| `index.ts` | 313 | B | `no-sandbox` 默认开、CSP `connect-src https:` 通配过宽 |
| `preload.js` | 200 | B | `window.harness` 别名、无 TS 类型 |
| `permissions/permission-gate.ts` | 162 | C | **deny 规则可绕过（Blocker）**、`.env` 仅拦 write |
| `permissions/permission-ask.ts` | 130 | A | 设计清晰、有超时保护 |
| `skills/skill-loader.ts` | 285 | B | watcher 数量无上限、单项目缓存 |
| `agents/roles.ts` | 184 | B | 模型名硬编码日期后缀（已部分修复） |
| `provider-validator.ts` | 113 | A | 完美遵循 SDK first |
| `user-settings.ts` | 230 | B | `compactionEnabled` 双写、`readUi` 不感知外部变更 |
| `migrate-settings.ts` | 95 | A | 幂等保护良好 |
| `shell-env-loader.ts` | 75 | C | **未在 index.ts 调用（功能失效）**、`SHELL` 无白名单 |
| `updater.ts` | 110 | A | 懒加载清晰 |
| `user-profile-service.ts` | 120 | C | 无原子写、prototype pollution 风险 |
| `shared/types.ts` | 562 | B | deprecated 类型混居 |
| `shared/message-convert.ts` | 115 | B | `as any` side-channel 注入 |
| `shared/look-storage.ts` | 90 | A | 路径定义清晰 |

### 2.2 主进程 Blocker 级 Bug

**MB-1：**`saveIndex` **/** `saveProjects` **非原子写**

- 位置：`agent-manager.ts:516-540, 249-263`
- 触发：主进程 SIGKILL/OOM/强制退出时 JSON 截断
- 影响：用户重启后所有 agent 全部"消失"
- 修复：tmp + rename 原子替换（约 30 行改动，可批量到所有 `writeFileSync` 调用）

**MB-2：macOS 关窗重开 IPC 失效**

- 位置：`index.ts:272-276` `app.on("activate")`
- 触发：macOS 用户关闭窗口后从 Dock 重新点开
- 影响：新窗口收不到任何 agent 事件，渲染层冻结
- 修复：`activate` 回调中调 `registerIpcHandlers(agentManager, mainWindow)`

**MB-3：**`permission-gate.ts` **全局 deny 规则可绕过**

- 位置：`permission-gate.ts:26-38`
- 绕过样例：`rm -rf /`（双空格）、`(rm -rf /)`、`rm -r -f /`、`eval "rm -rf /"`、bash 脚本间接执行
- 影响：作为最后安全屏障的全局 deny 形同虚设
- 修复：cmd 规范化（合并空格、去括号、检测 eval）+ 正则匹配

**MB-4：**`.env` **写保护仅覆盖** `write` **工具**

- 位置：`permission-gate.ts:45-59`
- 绕过样例：`edit` 工具改 `.env.local`、`bash` 中 `echo X >> .env` / `sed -i ... .env`
- 影响：敏感凭据文件可被 agent 静默修改
- 修复：扩展规则覆盖 `edit` 工具 + bash 中 `.env` + 写操作（`>`/`>>`/`tee`/`sed -i`）

### 2.3 主进程 Critical 级 Bug

- **MC-1**：`firstUserMessage` Map 在 `destroyAgent` 时不清理（内存泄漏）
- **MC-2**：`shell-env-loader.ts` 的 `loadShellEnv` **未在** `index.ts` **中被调用**——Dock 启动时 shell API key 注入功能完全失效
- **MC-3**：`shell-env-loader.ts` 的 `execSync` 对 `$SHELL` 无白名单，受控环境存在命令注入
- **MC-4**：IPC `look:invoke` 对 renderer 入参无运行时校验，renderer 被攻击时可传任意 path/cwd

### 2.4 重复造轮子清单（应优先用 SDK 原生）

| ID | 当前实现 | SDK 原生替代 | 文件 |
| --- | --- | --- | --- |
| RW-1 | 自建 title 生成 + completeSimple | `AgentSession.setSessionName` + 临时 inMemory session | `agent-manager.ts:1341-1380` |
| RW-2 | 手算 `getContextUsage` + `lastContextTokens` Map | `AgentSession.getContextUsage()` | `agent-manager.ts:1096-1133` |
| RW-3 | `agentUsage` Map 累计用量 + `trackUsage` | `AgentSession.getSessionStats()` | `agent-manager.ts:190, 2275-2307` |
| RW-4 | 手维护 `AgentStatus` 状态机 | SDK `agent_start/end` + `tool_execution_*` 事件驱动 | `agent-manager.ts:2313-2318` |
| RW-5 | `message_update` 本地拼接（已死代码） | 渲染层 pass-through 即可 | `agent-manager.ts:1686-1726` |
| RW-6 | tool-result backfill 双重 O(n²) | SDK `AgentSession.messages` 已关联 | `agent-manager.ts:577-596, 2247-2268` |

---

## 三、渲染层模块质量审查

### 3.1 [note.md](http://note.md) 五大架构问题现状

| 问题 | 状态 | 说明 |
| --- | --- | --- |
| State Monolith（App.tsx 状态单体） | ✅ 已修复 | 全面迁移到 Jotai，App.tsx 仅剩 1 个 useState |
| 无虚拟滚动（220 组件挂载） | ❌ 未修复（P0） | `ChatPanel.tsx:691` 仍是 `displayMessages.map(...)`，react-virtuoso 未集成 |
| 109 个独立 setTimeout | ❌ 部分缓解 | `scheduleCollapse` 模块级方案未实施，且新增 `ExecutionProcess` 的 300ms 定时器叠加 |
| IPC 事件在 App.tsx 处理 | ✅ 已修复 | 全面迁移到 `store/ipcHandler.ts`，appStore.set 直写 atom |
| React DevTools profiling 开销 | ⚠️ DEV 模式仍存在 | `react-scan` 在 DEV 模式启用，生产无影响 |

### 3.2 渲染层 Blocker / Critical 级 Bug

**RB-1：**`ProfileEditor.tsx:62` **头像上传时 Supabase 写入旧 avatar 值（闭包陷阱）**

```ts
reader.onload = () => {
    const dataUrl = reader.result;
    setProfile({ ...profile, avatar: dataUrl });  // 异步 setState
    supabase.upsert({ avatar: profile.avatar });  // ← 用了旧 profile 的闭包值！
};
```

- 触发：每次上传头像后同步 Supabase
- 影响：头像永远不更新到云端，本地看到新头像但其他设备/重装后丢失
- **修复：第 62 行改为** `avatar: dataUrl`

**RB-2：**`ipcHandler.ts:301` **流式消息 ID 不匹配导致内容错乱**

```ts
id: msg.id ?? `m_${Date.now()}_${Math.random().toString(36).slice(2, 6)}`
```

- 触发：主进程 `agent:message_end` 用真实 SDK ID，而 `message_start` 这里用了 fallback ID → `findIndex` 返回 -1 → 走 fallback 找 last streaming → 多消息并发流式时拼到错误消息上
- 影响：流式回复内容错乱（多 agent 并发或 steering 时）
- **修复：移除 fallback ID，要求主进程必须传 ID（或用 SDK ID 作为唯一来源）**

**RB-3：**`ChatPanel.tsx:245-246` `displayMessages` **的 useMemo 直接修改输入数组**

```ts
const chunks = a.assistantChunks;
chunks[c] = { ...chunks[c], ... };  // ← 突变上游 atom 数据
```

- 触发：tool result 注入时
- 影响：违反 React 状态不可变性，`useMemo` 比较依据失效，可能在长对话中产生间歇性 UI 不一致
- **修复：用** `chunks.map()` **或 immer 创建新数组**

**RB-4：**`PermissionDialog.tsx:72` **Esc 关闭 = 自动 deny**

```ts
onOpenChange={(o) => { if (!o) onDeny(); }}
```

- 触发：用户按 Esc 想暂时忽略弹窗
- 影响：Agent 工作流被静默中断，用户难以理解为何 agent 突然停止
- **修复：Esc 路径改为不响应（deny 必须是显式按钮点击）**

**RB-5：**`SettingsDialog.tsx:323` `appVersion = "1.0.0"` **硬编码**

- 影响：About tab 永远显示 v1.0.0，与实际打包版本无关
- **修复：通过 IPC 拉取** `app.getVersion()` **或注入** `import.meta.env.VITE_APP_VERSION`

### 3.3 渲染层 Major 级问题汇总

**性能（P0/P1 未修）**

- `ChatPanel.tsx:691` 全量渲染所有消息，220 组件同时挂载（agent 切换 760ms 卡顿仍存在）
- `ToolCallCard.tsx:135` + `ExecutionProcess.tsx:41-56` 双层 300ms 定时器（N + N×M 个并发 setTimeout）
- `ipcHandler.ts` 流式事件每次都 `[...messages]` 浅拷贝整个数组，长对话内存分配压力大
- `ChatPanel.tsx:223-338` `displayMessages` 的 `attachToolResult` 是 O(n²)（双层循环）

**i18n 缺失（30+ 处硬编码）**

| 文件 | 行号 | 问题文本 | 影响 |
| --- | --- | --- | --- |
| `ToolCallCard.tsx` | 213 | `字符` 硬编码中文 | 英/日界面看到中文 |
| `SettingsDialog.tsx` | 796 | `复杂认证` 硬编码中文 | 同上 |
| `SettingsDialog.tsx` | 209-237 | UpdateCheck 全部英文（"Download Update" 等） | 中/日界面看到英文 |
| `ThinkingSelector.tsx` | 11-17 | `LEVELS` 数组（Off/Minimal/Low/Medium/High/X-High + 描述）全部英文 | 中/日界面看到英文 |
| `PermissionModeSelector.tsx` | META 对象 | Ask/Plan/Allow 标签和描述全部英文 | 同上 |
| `SkillSlashMenu.tsx` | 235-342 | 8+ 处英文 | 同上 |
| `UpdateNotification.tsx` | 全部 | 7 处英文 | 同上 |
| `WelcomeScreen.tsx` | 21-26 | i18n key `welcome.title/description/openProject` 在三个 locale 文件中**全部缺失**，仅靠 `t(key, "default")` fallback | 同上 |
| `Sidebar.tsx` | 296 | `userName || "You"` 硬编码 | 同上 |
| `ContextRing.tsx` | 125 | tooltip "Compressing…" / "Click to compress" | 同上 |
| `MermaidBlock.tsx` | 196-199 | "Rendering diagram…" | 同上 |
| `App.tsx` | 355-367 | `!api` 错误状态 "Harness API not available." | 同上 |

**ja.json 缺失关键 key**

- `settings.agentName`、`settings.agentNameDesc`、`settings.agentNamePlaceholder` 在 ja.json 中无定义

**IPC 健壮性问题**

- `App.tsx:116-119` `handleDeleteProject` 无 `.catch`
- `SettingsDialog.tsx:468-475` `handleResetDefaults` 无 `.catch`
- `ipcHandler.ts:493/499/512` 多处 `.catch(() => {})` 静默吞错
- `ipcHandler.ts:510-519` `listProjects` 与 `getAgents` 并发，`_autoSelectAgent` 可能在 project 状态未稳定前执行，冷启动可能选错 agent

**安全相关**

- `MermaidBlock.tsx:85` `node.innerHTML = svg` 未 sanitize（虽然 Mermaid `securityLevel: sandbox` 已缓解，但仍建议加 DOMPurify）
- `StreamingMarkdown.tsx` 的 `MarkdownA` 组件添加 `target="_blank"`，但需要确认主进程有 `setWindowOpenHandler` 拦截，否则可能在 Electron 窗口内打开外部 URL

**UX 摩擦**

- `ContextRing.tsx:37-58` 3 秒轮询 + IPC 事件双通道**冗余**（每 3 秒触发一次额外渲染）
- `SettingsDialog.tsx` 每次打开都重复调 `getGeneralSettings`（与 `initAppData` 重复）
- `ModelSelector.tsx` 每次打开都重新 `fetchModels`（无缓存 TTL）

### 3.4 渲染层评分总表

| 文件 | 评分 | 主要问题 |
| --- | --- | --- |
| `index.tsx` | B | react-scan 无条件引入 |
| `App.tsx` | B+ | i18n、IPC 静默失败 |
| `store/atoms.ts` | A | atomFamily GC 隐患（已正确处理） |
| `store/ipcHandler.ts` | B+ | RB-2 流式 ID 问题、并发竞态 |
| `ChatPanel.tsx` | B | RB-3 mutation、O(n²) 算法、虚拟滚动缺失 |
| `MessageBubble.tsx` | A- | 内联 ContentBlocks 未 memo |
| `ToolCallCard.tsx` | B+ | P1 定时器问题、`字符`硬编码 |
| `ThinkingPanel.tsx` | A- | "Reasoning" 硬编码 |
| `ExecutionProcess.tsx` | B+ | 双层定时器 |
| `StreamingMarkdown.tsx` | A- | useThrottle 双 flush |
| `Sidebar.tsx` | B+ | 重复派生、project 切换不滚动 bug |
| `PermissionDialog.tsx` | A- | RB-4 Esc=deny |
| `SettingsDialog.tsx` | B | RB-5 版本硬编码、12 处 i18n 缺失、重复 IPC |
| `ModelSelector.tsx` | A- | 双击切换竞态 |
| `ContextRing.tsx` | B | 双通道冗余 |
| `ThinkingSelector.tsx` | A- | i18n 全缺失 |
| `PermissionModeSelector.tsx` | B+ | i18n 全缺失 |
| `SkillSlashMenu.tsx` | B+ | i18n 全缺失 |
| `UpdateNotification.tsx` | B | i18n 全缺失 |
| `WelcomeScreen.tsx` | B+ | locale key 缺失 |
| `MermaidBlock.tsx` | B+ | innerHTML 未 sanitize |
| `ProfileEditor.tsx` | B | RB-1 闭包 bug |
| `LoginScreen.tsx` | B+ | UX 微缺失 |

---

## 四、综合整改路线图

### P0 — 必须立即修复（Blocker，影响数据安全/功能性正确）

| ID | 项 | 文件 | 工作量 |
| --- | --- | --- | --- |
| MB-1 | 索引 JSON 原子写（tmp+rename） | `agent-manager.ts:516-540, 249-263` | 2h |
| MB-2 | macOS `activate` 重注册 IPC | `index.ts:272-276` | 1h |
| MB-3 | 全局 deny 规则规范化抗绕过 | `permission-gate.ts:26-38` | 4h |
| MB-4 | `.env` 写保护扩展到 edit/bash | `permission-gate.ts:45-59` | 3h |
| RB-1 | ProfileEditor 头像闭包 bug | `ProfileEditor.tsx:62` | 10min |
| RB-2 | 流式消息 ID fallback 移除 | `ipcHandler.ts:301` | 1h |
| RB-5 | SettingsDialog 版本号动态获取 | `SettingsDialog.tsx:323` | 30min |
| 测试 | 给 permission-gate 补单元测试（含绕过 case） | `test/` | 4h |

### P1 — 强烈建议（Critical，影响安全/稳定性/SDK 升级风险）

| ID | 项 | 文件 | 工作量 |
| --- | --- | --- | --- |
| 安全 | `no-sandbox` 反转默认（仅 opt-in） | `index.ts:28-32` | 30min |
| 安全 | CSP `connect-src https:` 移除通配 | `index.ts:129` | 1h |
| 安全 | 删除 `window.harness` 别名 | `preload.js:197-198` | 2h（需迁移调用） |
| 安全 | IPC 入参运行时校验（zod or guards） | `ipc-handlers.ts:27` | 1d |
| MC-1 | `firstUserMessage` Map destroyAgent 清理 | `agent-manager.ts:1261` | 10min |
| MC-2 | `loadShellEnv` 在 index.ts 调用（异步） | `index.ts` + `shell-env-loader.ts` | 2h |
| MC-3 | shell-env-loader $SHELL 白名单 | `shell-env-loader.ts:38` | 30min |
| 性能 | 删除主进程 message_update 死代码 | `agent-manager.ts:1685-1726` | 1h |
| 性能 | message_update IPC 节流（16ms 批合并） | `agent-manager.ts:1683-1726` | 4h |
| RB-3 | displayMessages mutation 修复 | `ChatPanel.tsx:245-246` | 30min |
| RB-4 | PermissionDialog Esc 不响应 deny | `PermissionDialog.tsx:72` | 15min |
| 性能 P0 | 引入 react-virtuoso/tanstack-virtual 虚拟滚动 | `ChatPanel.tsx` | 2-3d |
| 性能 P1 | `scheduleCollapse` 批量定时器 | `ToolCallCard.tsx`, `ExecutionProcess.tsx` | 0.5d |

### P2 — 建议改进（Major，影响维护性/UX）

- 提取 `TitleGeneratorService` / `ProjectStore` / `SessionTreeService`（消除 `agent-manager.ts` 的 God Object）
- 拆分 `shared/types.ts` 为多文件
- `handleSessionEvent` 类型从 `any` 改为 `AgentSessionEvent`（启用 TS 检查 SDK 升级影响）
- `ipc-handlers.ts` 按域拆分子模块
- ContextRing 移除 3s 轮询（保留 IPC 事件）
- i18n 全面补全（30+ 硬编码 + ja.json 缺失 key + welcome.\* locale 缺失）
- 替换 `(window as any).look` 为类型化 `.d.ts` 声明
- `compactionEnabled` 双写收敛到 SDK 路径
- 用 SDK 原生能力替换 RW-1 \~ RW-6 重复造轮子

### P3 — 长期优化

- 完整测试金字塔：permission-gate / message-convert 单测 + IPC 集成测试 + Playwright E2E
- GitHub Actions CI 跑 `npm run check`
- `agents.json + projects.json` 合并为单一原子写入文件
- `agent:history` 分页推送（避免长会话大 payload）
- 标题生成迁移到 SDK SessionManager.inMemory + AgentSession
- Supabase 双轨认证流程明确化（产品决策层面）
- 主进程 dev 增加 tsc --watch + electron-reload

---

## 五、SDK 升级兼容性评估

**当前依赖**：`^0.79.1`（接受 0.79.x patch，不接受 0.80.x）

**升级到 0.80.x 的影响面**（按风险递减）：

| 接触面 | 文件 | 风险 | 升级时需验证 |
| --- | --- | --- | --- |
| `AgentSessionEvent` switch（250 行 any 类型） | `agent-manager.ts:1660-1909` | 高 | 每个 event.type 字段 |
| `SessionManager` 内部方法（getBranch/getLeafId/getTree/resetLeaf/branch/appendLabelChange） | `agent-manager.ts` 多处 | 高 | 每个方法签名 |
| `extractMessagesFromSessionManager` 的 `sm: any` | `agent-manager.ts:2215` | 高 | 整个方法 |
| `pi-ai` 底层 `completeSimple` | `agent-manager.ts`, `provider-validator.ts` | 中 | 函数签名 + 返回结构 |
| `extensionFactories` 的 tool_call hook | `agent-manager.ts:2404-2456` | 中 | event 字段名 |
| `createAgentSession` 入参 | `agent-manager.ts` 集中 | 低 | SDK 通常向后兼容 |
| `AuthStorage` / `ModelRegistry` | 通过 wrapper | 低 | 高级 API 稳定 |
| Skills | `skill-loader.ts` | 低 | 已封装 |

**升级前置条件**：

1. 至少补齐 `permission-gate` 和 `message-convert` 单测，避免无声回归
2. `handleSessionEvent` 类型化（移除 any）
3. 提取 `SessionTreeService` 包装 SessionManager 内部方法

---

## 六、最终交付物清单

| 交付物 | 路径 | 状态 |
| --- | --- | --- |
| 全面技术审查报告 | `workspace-files/.context/2026-06-15-comprehensive-tech-review.md` | ✅ 当前文件 |
| Provider/API Keys 专项审计 | `workspace-files/.context/monday-june-15-delegated-spring.md` | ✅ 已存在 |
| 性能架构旧分析 | `workspace-files/.context/note.md` | ✅ 已存在 |

**报告的预期使用方式**：

- 短期：按 P0 清单逐项修复（约 1-2 周）
- 中期：P1 清单滚动推进（1-2 月，配合发版节奏）
- 长期：P2/P3 作为重构 backlog

**未在本次审查覆盖的方面**（建议后续单独审查）：

- 实际运行时性能（需要 react-scan 实测 + 大会话压力测试）
- Supabase schema 与 RLS 策略
- electron-builder 公证流程的实操配置
- 多语言用户的实际 UX 测试
- Skills 系统的 [SKILL.md](http://SKILL.md) 解析在边缘情况下的行为