# Look UI 修复计划

> 基于 2026-06-15 UI 审查报告的完整修复方案
> 共 19 个任务，分 4 个 Phase

---

## Phase 0：架构拆分（P0，先做，否则后续改动都在这两个巨型文件上叠床架屋）

### Task 0.1 — 拆分 ChatPanel.tsx → 4 个文件
**现状**：~1010 行，包含 Virtuoso、Slash menu、Queue drawer、输入框、Branch confirm 全部逻辑
**目标**：
```
src/renderer/components/
├── ChatPanel.tsx           # 主组件，~250 行（只做编排）
├── ChatMessageList.tsx     # Virtuoso + 空状态 + ScrollToBottom
├── ChatInput.tsx           # Textarea + SkillOverlay + SlashMenu + Toolbar
└── ChatQueueDrawer.tsx     # Queue drawer（已有独立逻辑，抽出去即可）
```
**注意**：拆分时保持 props 接口不变，确保 ChatPanel 对 App.tsx 的接口零改动

### Task 0.2 — 拆分 SettingsDialog.tsx → 6 个文件
**现状**：~1038 行，包含 5 个 tab + API key 管理 + Update checker
**目标**：
```
src/renderer/components/settings/
├── SettingsDialog.tsx      # 主 dialog + Tabs 框架，~150 行
├── GeneralTab.tsx          # 通用设置
├── ApiKeysTab.tsx          # API key 管理（最复杂的一个）
├── ChatPromptTab.tsx       # 聊天提示词设置
├── ProfileTab.tsx          # 用户资料编辑
└── AboutTab.tsx            # 关于 + 更新检查
```

---

## Phase 1：无障碍 & 暗色模式（P1，影响面最广）

### Task 1.1 — 添加 `prefers-reduced-motion` 降级
**文件**：`src/renderer/App.css`
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```
- 位置：在 `@layer components` 之后，`/* Unlayered overrides */` 之前

### Task 1.2 — 修复暗色模式下 app-shell 阴影
**文件**：`src/renderer/App.css`
```css
/* 在 .dark 块中添加 */
.dark .app-shell {
  box-shadow: inset 280px 0 120px -100px oklch(1 0 0 / 0.06);
}
```

### Task 1.3 — 暗色模式滚动条优化
**文件**：`src/renderer/App.css`
```css
.dark ::-webkit-scrollbar-thumb { background: oklch(0.55 0.002 85); }
.dark ::-webkit-scrollbar-thumb:hover { background: oklch(0.62 0.002 85); }
```

### Task 1.4 — Assistant 消息添加 max-width
**文件**：`src/renderer/components/MessageBubble.tsx`
- 将 `isAssistant && "whisper-bubble--assistant w-full"` 改为 `max-w-[85%]`

### Task 1.5 — PixelAgentAvatar role label 国际化
**文件**：`src/renderer/components/PixelAgentAvatar.tsx`
- 移除硬编码的 `ROLE_LABEL` 对象
- 改用 `useTranslation()` + i18n keys: `roles.chat`, `roles.crawler` 等
- 在 `locales/en.json`, `zh.json`, `ja.json` 中添加对应 keys

### Task 1.6 — 消息时间戳
**文件**：`src/renderer/components/MessageBubble.tsx`
- 在 sender label 行添加 `timestamp` 显示
- 格式：相对时间（`now`, `2m`, `1h`）+ hover 显示绝对时间
- `PiMessage` 类型需要确认是否已有 `timestamp` 字段

---

## Phase 2：UX 打磨（P1-P2）

### Task 2.1 — 统一设置保存行为
**文件**：`src/renderer/components/settings/*.tsx`
- General tab 所有 switch/select 改为即时保存（去掉 Save 按钮依赖）
- Save 按钮改为 Close 按钮（或直接去掉，用 X 关闭）
- API key 保持现有即时保存逻辑

### Task 2.2 — LoginScreen "记住我"
**文件**：`src/renderer/components/LoginScreen.tsx`
- 添加 `<Switch>` + label "Remember me"
- 勾选时调用 `supabase.auth.signInWithPassword` 后设置 `persistSession: true`
- Supabase client 配置检查 `auth.persistSession`

### Task 2.3 — 空聊天界面引导
**文件**：`src/renderer/components/ChatMessageList.tsx`（拆分后）
- 替换当前的简单占位文字
- 添加 3 个快捷示例卡片（如 "Explain this code", "Write a function", "Debug an error"）
- 点击示例卡片自动填入输入框并 send

### Task 2.4 — Sidebar 右键菜单
**文件**：`src/renderer/components/Sidebar.tsx`
- 使用已有的 `DropdownMenu` 组件
- 右键菜单项：Rename / Delete / Copy Agent ID / Export Chat
- 绑定 `onContextMenu` 事件

### Task 2.5 — Sidebar scrollbar 可见性
**文件**：`src/renderer/components/Sidebar.tsx`
- 移除 `[&_[data-slot=scroll-area-scrollbar]]:hidden`
- 或者保留隐藏但添加底部渐变 fade 指示器（更优雅）

### Task 2.6 — ContextRing tooltip 用 HTML tooltip
**文件**：`src/renderer/components/ContextRing.tsx`
- 用已有的 `<Tooltip>` 组件包裹
- 内容分两行：百分比 + "Click to compress"

---

## Phase 3：性能 & 细节（P2）

### Task 3.1 — ModelSelector 缓存模型列表
**文件**：`src/renderer/components/ModelSelector.tsx`
- 用 `useRef` 缓存上次 fetch 结果
- `onOpenChange` 时如果缓存存在且未过期（比如 60s），不重新 fetch
- provider 变化时 invalidate 缓存

### Task 3.2 — safeJson 缓存
**文件**：`src/renderer/components/ToolCallCard.tsx`
- 用 `useMemo` 包裹 `safeJson(toolCall.args)`
- 依赖 `toolCall.args`（对象引用比对）

### Task 3.3 — UserAvatar border-radius 统一
**文件**：`src/renderer/components/UserAvatar.tsx`
- `img` 和 fallback div 统一使用 `rounded-lg`

### Task 3.4 — Settings dialog forceMount 清理
**文件**：`src/renderer/components/settings/SettingsDialog.tsx`
- 移除 `forceMount` + `data-[state=closed]:hidden` workaround
- 如果 DialogContent 卸载有问题，检查 Radix 版本

---

## 执行顺序建议

```
Phase 0 (Task 0.1, 0.2)   → 先拆分，降低后续修改风险
Phase 1 (Task 1.1-1.6)     → 无障碍 + 暗色，影响面广
Phase 2 (Task 2.1-2.6)     → UX 打磨，用户感知最强
Phase 3 (Task 3.1-3.4)     → 收尾细节
```

---

## 预估工作量

| Phase | 任务数 | 预估时间 |
|-------|--------|----------|
| Phase 0 | 2 | 2-3h |
| Phase 1 | 6 | 3-4h |
| Phase 2 | 6 | 3-4h |
| Phase 3 | 4 | 1-2h |
| **合计** | **18** | **9-13h** |
